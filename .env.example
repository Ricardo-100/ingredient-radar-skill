# 成分雷达 · 环境配置示例

复制成 `.env` 或直接在 shell 里 export。**`.env` 已被 .gitignore 忽略，不要提交。**

```bash
# ---- 可复现 ----
IR_SEED=7
IR_TEMPERATURE=0

# ---- 产物 ----
# 默认 <skill>/output，一般不用设。
# ⚠ 若你改成相对路径，它是**相对当前工作目录**、不是相对 skill 目录：
#   在别的目录里跑 `python /path/to/ingredient-radar/scripts/main.py` 时，
#   产物会落到那个目录去。跨目录调用请写绝对路径，或干脆留空不设。
# IR_OUT_DIR=./output

# ---- 调试题 ----
IR_DEBUG=0
```

## 没有端点、没有 API Key、没有模型名

2026-09-25 起本 skill 移除了全部 HTTP 代码（原 `scripts/llm_client.py`、
`scripts/backends/` 整个目录）。第①②步（定位四区 + 逐区抽取）由 **Agent 自己看图**
产出，不经过任何推理端点。

所以原先这些变量**全部取消**，不是改名：

| 原变量 | 去向 |
|--------|------|
| `IR_BASE_URL` | 删除——没有端点要配 |
| `IR_API_KEY` | 删除——没有 Key 要用 |
| `IR_MODEL` | 删除——模型就是 Agent 自己 |
| `IR_BACKEND` | 删除——只有一种视觉来源 |
| `IR_TIMEOUT` / `IR_MAX_TOKENS` / `IR_RETRIES` | 删除——不发请求 |
| `IR_ENABLE_THINKING` / `IR_THINKING_*` | 删除——Qwen 推理开关，随之不再需要 |

副作用：`config.snapshot()` 天然不含密钥字段——不是「显式排除」，是本来就没有。

## Windows PowerShell 设置方式

```powershell
$env:IR_SEED = "7"
```
